{
  "scripts": {
    "serve": "concurrent --kill-others \"goapp serve app_local.yaml\" --host=0.0.0.0",
    "serve-prod": "concurrent --kill-others \"goapp serve app.yaml\"",
    "doc": "concurrent --kill-others \"godoc -http=:6060\"",
    "tests": "goapp test",
    "rollback": "appcfg.sh rollback",
    "deploy": "goapp deploy",
    "fmt": "goapp fmt"
  },
  "betterScripts": {
  },
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^0.1.1"
  },
  "engines": {
    "node": "4.1.1"
  }
}
